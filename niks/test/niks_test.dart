import 'package:flutter_test/flutter_test.dart';

import 'package:niks/niks.dart';

void main() {
  test('adds one to input values', () {
    Niks skin = Niks.blank(NiksOptions(width: 300, height: 300));
  });
}
